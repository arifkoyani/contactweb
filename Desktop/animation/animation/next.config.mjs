/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['images.unsplash.com'], // Allow Unsplash domain for next/image
    },
  };
  
  export default nextConfig;
  